---
- name: Proxmox 템플릿 기반 VM 일괄 생성 (매핑 에러 해결)
  hosts: localhost
  gather_facts: false
  
  vars:
    # 설문 조사에서 어떤 텍스트를 선택하든 대응할 수 있도록 키를 보완합니다.
    spec_map:
      "Small (1 core / 2GB)": { "cpu": 1, "ram": 2048 }
      "Medium (2 cores / 4GB)": { "cpu": 2, "ram": 4096 }
      "Large (4 cores / 8GB)": { "cpu": 4, "ram": 8192 }
      # 혹시 모를 예외 상황을 위해 숫자 키도 대비합니다.
      "1": { "cpu": 1, "ram": 2048 }
      "2": { "cpu": 2, "ram": 4096 }
      "4": { "cpu": 4, "ram": 8192 }

    raw_vm_count: "{{ survey_vm_count | default('1') }}"
    vm_name_prefix: "{{ survey_vm_name_prefix | default('new-vm') }}"
    target_node: "{{ survey_target_node | default('xxhci01') }}"
    template_name: "{{ survey_template_name }}"
    selected_spec: "{{ survey_vm_spec }}"
    selected_vnet: "{{ survey_vm_vnet }}"

  tasks:
    - name: "[확인] 입력된 변수 값 디버깅"
      debug:
        msg: 
          - "선택된 사양: {{ selected_spec }}"
          - "대상 노드: {{ target_node }}"
          - "VNet: {{ selected_vnet }}"

    - name: "[생성] {{ raw_vm_count }}대의 VM 복제 및 설정 시작"
      community.proxmox.proxmox_kvm:
        api_user: "{{ lookup('env', 'PROXMOX_USER') }}"
        api_token_id: "{{ lookup('env', 'PROXMOX_TOKEN_ID') }}"
        api_token_secret: "{{ lookup('env', 'PROXMOX_TOKEN_SECRET') }}"
        api_host: "{{ lookup('env', 'PROXMOX_URL') | regex_replace('https://|:8006', '') }}"
        validate_certs: false
        
        node: "{{ target_node }}"
        clone: "{{ template_name }}"
        name: "{{ vm_name_prefix }}-{{ item }}"
        full: true
        timeout: 600
        
        # spec_map에서 값을 찾지 못할 경우를 대비해 default를 지정합니다.
        cores: "{{ spec_map[selected_spec].cpu | default(2) }}"
        memory: "{{ spec_map[selected_spec].ram | default(4096) }}"
        
        netif: '{"net0":"virtio,bridge={{ selected_vnet }}"}'
        state: present
      loop: "{{ range(1, (raw_vm_count | int) + 1) | list }}"
