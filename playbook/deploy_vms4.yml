---
- name: Proxmox VM 일괄 생성 (최종 안정화 버전)
  hosts: localhost
  gather_facts: false

  vars:
    # 템플릿 이름에서 ID 추출
    raw_template: "{{ survey_template_name | default('106') }}"
    template_id: "{{ raw_template | regex_replace('^(\\d+).*', '\\1') | int }}"

  tasks:
    - name: "[준비] 필수 라이브러리(proxmoxer) 설치"
      pip:
        name: [proxmoxer, requests]
        state: present

    - name: "[단계 1] VM 복제 생성 (하드웨어 승계 보장)"
      community.proxmox.proxmox_kvm:
        api_user: "{{ lookup('env', 'PROXMOX_USER') }}"
        api_token_id: "{{ lookup('env', 'PROXMOX_TOKEN_ID') }}"
        api_token_secret: "{{ lookup('env', 'PROXMOX_TOKEN_SECRET') }}"
        api_host: "{{ lookup('env', 'PROXMOX_URL') | regex_replace('https://|:8006', '') }}"
        validate_certs: false
        
        node: "xxhci00"   # 원본 템플릿 위치
        vmid: "{{ template_id }}"
        target: "xxhci01"
        name: "KFE-VM-{{ item }}"
        full: true
        state: present
      # 숫자를 안전하게 처리하는 with_sequence 사용 [cite: 2026-02-28]
      with_sequence: "start=1 end={{ survey_vm_count | default(1) }}"

    - name: "[단계 2] 생성된 VM 전원 켜기"
      community.proxmox.proxmox_kvm:
        api_user: "{{ lookup('env', 'PROXMOX_USER') }}"
        api_token_id: "{{ lookup('env', 'PROXMOX_TOKEN_ID') }}"
        api_token_secret: "{{ lookup('env', 'PROXMOX_TOKEN_SECRET') }}"
        api_host: "{{ lookup('env', 'PROXMOX_URL') | regex_replace('https://|:8006', '') }}"
        validate_certs: false
        node: "xxhci01"
        name: "KFE-VM-{{ item }}"
        state: started
      with_sequence: "start=1 end={{ survey_vm_count | default(1) }}"
